<odoo>
    <record id="view_partner_form_clean_patient" model="ir.ui.view">
        <field name="name">res.partner.form.clean.patient</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>

        <field name="arch" type="xml">

            <!-- Replace ENTIRE sheet -->
            <xpath expr="//sheet" position="replace">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="age"/>
                            <field name="gender"/>
                            <field name="phone"/>
                            <field name="email"/>
                            <field name="is_patient"/>
                        </group>
                        <group>
                            <field name="house"/>
                            <field name="place"/>
                            <field name="city"/>
                            <field name="zip" string="PIN Code"/>
                        </group>
                    </group>
                </sheet>
            </xpath>
        </field>
    </record>
</odoo>
